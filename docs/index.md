# Welcome to shrinkr

This is the documentation for `shrinkr`.

## Installation

```bash
pip install shrinkr
```

**`docs/api.md`** (Your auto-generated API reference):
To automatically pull in the docstrings from your `shrinkr` Python module (which likely wraps your C functions), use the `mkdocstrings` directive.

